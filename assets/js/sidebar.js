// Side Bar
// const transformBtn = document.querySelectorAll(".accordion-button");
/**
transformBtn.forEach((element) => {
    element.addEventListener("click", function () {
        if (element.className == "accordion-button") {
            const chevronDown = this.querySelector('.fa-chevron-down');
            chevronDown.classList.add("transFormSidebarBtn");
        }else{
            const chevronDown = this.querySelector('.fa-chevron-down');
            chevronDown.classList.remove("transFormSidebarBtn")
        }
    })
})
**/